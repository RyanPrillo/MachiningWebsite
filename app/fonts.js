import { Manrope, Roboto_Slab, Lora } from 'next/font/google'

export const robotoSlab = Roboto_Slab({
    subsets: ['latin'],
    display: 'swap',
})

export const manrope = Manrope({
    subsets: ['latin'],
    display: 'swap',
    weight: ["variable"]
})

export const lora = Lora({
    subsets: ['latin'],
    display: 'swap',
    weight: ["variable"]
})